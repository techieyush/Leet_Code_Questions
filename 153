class Solution {
    public int findMin(int[] nums) {
        int n=nums.length-1;
        int i=0;
        while(i!=n)
        {
            if(nums[i]>nums[i+1])
            {
                return nums[i+1];
            }
            i++;
        }
       return nums[0];
        
    }
}
