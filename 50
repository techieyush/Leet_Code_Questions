
import java.lang.Math;
class Solution {
    public double myPow(double x, int n) {
        double number=0;
        if(n<0)
        {
            number=Math.pow(x,n);
            return number;
        }
        else{
            number=Math.pow(x,n);
        }
        return number;
        
    }
}
